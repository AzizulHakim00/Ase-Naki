spring:
  datasource:
    url: jdbc:h2:mem:asenaki-test;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: create-drop
app:
  admin:
    password: TestAdmin123!
  monitoring:
    power-grid-url: https://example.test/power
    ioda-url: https://example.test/ioda
    cloudflare-url: https://example.test/cloudflare
    cloudflare-token:
    refresh-secret: test-refresh-secret
